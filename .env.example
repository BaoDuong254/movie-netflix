VITE_API_TOKEN=YOUR_API_TOKEN_HERE
# The API token is used to authenticate requests to the API.
# You can obtain it from the API provider's dashboard or settings page.
# Make sure to keep this token secret and do not share it publicly.
VITE_API_HOST=YOUR_BASE_API_URL_HERE
# The API host is the base URL for the API endpoints.
# It is used to construct the full URL for API requests.
# You can change this to point to a different API host if needed.
# Example: VITE_API_HOST=https://api.example.com/v1 